@using LidyCms.Common
@using LidyCms.Model
@using LidyCms.Web.UI
<!DOCTYPE html>
<html>
<head>
    <title>星恒星珠宝 K金、钻石、蓝玉髓、绿玉髓、和田白玉、碧玺、晶石、珍珠等</title>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <link href="@Url.Content("~/Content/Css/main.css")" rel="stylesheet/less" type="text/css" />
    <script src="@Url.Content("~/Content/Scripts/jquery-1.8.2.min.js")" type="text/javascript"></script>
    <script src="@Url.Content("~/Content/Scripts/jquery.easing.1.3.js")" type="text/javascript"></script>
    @if (IsSectionDefined("AddToHead"))
    {
        @RenderSection("AddToHead", required: false)
    }
</head>
<body>
    <!-- 导航 -->
    <div class="nav">
        <div class="top">
            <div class="logo">
                <a href="@Url.Action("Index", "Index")">
                    <img title="星恒星-恒星珠宝旗下品牌" src="/content/images/logo.png" alt="logo"></a>
            </div>
            <ul id="sdt_menu" class="sdt_menu">
				<li>
					<a href="@Url.Action("Index", "Index")">
						<img src="/content/images/1.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">首页</span>
							<span class="sdt_descr">Index</span>
						</span>
					</a>
				</li>
				<li>
					<a href="@Url.Action("Index", "Content", new { page = "about" })">
						<img src="/content/images/2.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">关于星恒星</span>
							<span class="sdt_descr">AboutUs</span>
						</span>
					</a>
					<div class="sdt_box">
                     @{
                        List<Contents> about = Utils.GetCacheContents((int)BasePage.Channel.Contents, 10);
                        foreach (Contents item in about)
                        {
                        <a href="@Url.Action("Index", "Content", new { page = item.CallIndex })">@item.Title</a>
                        }
                      }
					</div>
				</li>
				<li>
					<a href="@Url.Action("Index", "Picture", new { id = 4 })">
						<img src="/content/images/3.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">爱的礼物</span>
							<span class="sdt_descr">Gift of Love</span>
						</span>
					</a>
                    <div class="sdt_box">
                     @{
                        List<Channel> gift = Utils.GetCacheChannel((int)BasePage.Channel.Pictures, 3);
                        foreach (Channel item in gift)
                        {
                        <a href="@Url.Action("Index", "Picture", new { id = item.Id })">@item.Title</a>
                        }
                      }
                    </div>
				</li>
				<li>
					<a href="@Url.Action("Index", "Product", new { id =LidyCms.Web.UI.BasePage.Channel.Pruducts })">
						<img src="/content/images/4.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">产品系列</span>
							<span class="sdt_descr">Products</span>
						</span>
					</a>
                    <div class="sdt_box">
                     @{
                        List<Channel> products = Utils.GetCacheChannel((int)BasePage.Channel.Pictures, 15);
                        foreach (Channel item in products)
                        {
                           <a href="@Url.Action("Index", "Product", new { id = item.Id })">@item.Title</a>
                        }
                      }
                    </div>
				</li>
                	<li>
					<a href="@Url.Action("Index", "Content", new { page = "Advantage" })">
						<img src="/content/images/6.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">加盟星恒星</span>
							<span class="sdt_descr">Advantage</span>
						</span>
					</a>
                    <div class="sdt_box">
                     @{
                        List<Contents> advantage = Utils.GetCacheContents((int)BasePage.Channel.Contents, 12);
                        foreach (Contents item in advantage)
                        {
                           <a href="@Url.Action("Index", "Content", new { page = item.CallIndex })">@item.Title</a>
                        }
                      }
                    </div>
				</li>
				<li>
					<a href="@Url.Action("Index", "News", new { id = 6 })">
						<img src="/content/images/6.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">星恒星资讯</span>
							<span class="sdt_descr">News</span>
						</span>
					</a>
                    <div class="sdt_box">
                     @{
                        List<Channel> news = Utils.GetCacheChannel((int)BasePage.Channel.News, 5);
                        foreach (Channel item in news)
                        {
                           <a href="@Url.Action("Index", "News", new { id = item.Id })">@item.Title</a>
                        }
                      }
                    </div>
				</li>
				<li>
					<a href="@Url.Action("Index", "News", new { id = 6 })">
						<img src="/content/images/7.png" alt=""/>
						<span class="sdt_active"></span>
						<span class="sdt_wrap">
							<span class="sdt_link">星恒星博客</span>
							<span class="sdt_descr">blogs</span>
						</span>
					</a>
				</li>
			</ul>
            @*<ul class="navmenu clearfix" id="nav">
                <li><a href="@Url.Action("Index", "Index")">首页</a></li>
                <li><a href="@Url.Action("Index", "Content", new { page = "about" })">关于星恒星</a>
                    <ul>
                        @{
                            List<Contents> about = Utils.GetCacheContents((int)BasePage.Channel.Contents, 10);
                            foreach (Contents item in about)
                            {
                            <li><a href="@Url.Action("Index", "Content", new { page = item.CallIndex })">@item.Title</a></li>
                            }
                        }
                    </ul>
                </li>
                <li><a href="@Url.Action("Index", "Picture", new { id = 4 })">爱的礼物</a>
                    <ul id="nv3" style="display: none;">
                        @{
                            List<Channel> gift = Utils.GetCacheChannel((int)BasePage.Channel.Pictures, 3);
                            foreach (Channel item in gift)
                            {
                            <li><a href="@Url.Action("Index", "Picture", new { id = item.Id })">@item.Title</a></li>
                            }
                        }
                    </ul>
                </li>
                <li><a href="@Url.Action("Index", "Product", new { id = 16 })">产品系列</a>
                    <ul>
                        @{
                            List<Channel> products = Utils.GetCacheChannel((int)BasePage.Channel.Pictures, 15);
                            foreach (Channel item in products)
                            {
                            <li><a href="@Url.Action("Index", "Product", new { id = item.Id })">@item.Title</a></li>
                            }
                        }
                    </ul>
                </li>
                <li><a href="@Url.Action("Index", "Content", new { page = "Advantage" })">加盟星恒星</a>
                    <ul>
                        @{
                            List<Contents> advantage = Utils.GetCacheContents((int)BasePage.Channel.Contents, 12);
                            foreach (Contents item in advantage)
                            {
                            <li><a href="@Url.Action("Index", "Content", new { page = item.CallIndex })">@item.Title</a></li>
                            }
                        }
                    </ul>
                </li>
                <li><a href="@Url.Action("Index", "News", new { id = 6 })">星恒星资讯</a>
                    <ul>
                        @{
                            List<Channel> news = Utils.GetCacheChannel((int)BasePage.Channel.News, 5);
                            foreach (Channel item in news)
                            {
                            <li><a href="@Url.Action("Index", "News", new { id = item.Id })">@item.Title</a></li>
                            }
                         }
                    </ul>
                </li>
                <li><a href="@Url.Action("Index", "News")">星恒星博客</a></li>
            </ul>*@
        </div>
    </div>
   <script src="@Url.Content("~/Content/Scripts/jquery.min.js")" type="text/javascript"></script>
    @RenderBody()
    <div class="footer">
        <div class="c1201">
            <ul>
                @{
                    List<Contents> contact = Utils.GetCacheContents((int)BasePage.Channel.Contents, 23);
                    foreach (Contents item in contact)
                    {
                    <li><a href="@Url.Action("Index", "Content", new { page = item.CallIndex })">@item.Title</a></li>
                    }
                }
            </ul>
            <span>Copyrights@深圳市恒星珠宝有限公司 服务热线4008-XXX-XXX</span>
        </div>
    </div>
    <script type="text/javascript">
        // 二级菜单
        $("#nav").on({
            mouseenter: function () {
                $self = $(this);
                $self.find("ul").stop(true).slideDown();
            },
            mouseleave: function () {
                $self = $(this);
                $self.find("ul").slideUp();
            }
        },
            ">li");
    </script>
</body>
</html>
